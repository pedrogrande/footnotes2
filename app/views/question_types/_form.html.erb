<%= simple_form_for(@question_type) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :description %>
    <%= f.input :num_of_answers_required %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
